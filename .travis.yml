sudo: required
services:
  - docker

before_install:
  - docker build -t franciscojaviermartin/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run franciscojaviermartin/react-test npm test -- --coverage

after_success:
  - docker build -t franciscojaviermartin/multi-client ./client
  - docker build -t franciscojaviermartin/multi-nginx ./nginx
  - docker build -t franciscojaviermartin/multi-server ./server
  - docker build -t franciscojaviermartin/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push franciscojaviermartin/multi-client
  - docker push franciscojaviermartin/multi-nginx
  - docker push franciscojaviermartin/multi-server
  - docker push franciscojaviermartin/multi-worker